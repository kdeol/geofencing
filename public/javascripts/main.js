import React from 'react';
import ReactDOM from 'react-dom';
import MainContainer from '../../ui/containers/MainContainer.jsx';

// Render the react app in our main container
ReactDOM.render(
  <MainContainer />,
  document.getElementById('main')
);